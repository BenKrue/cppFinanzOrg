#pragma once

namespace enums {
    enum class categoryType { 
        RECIPIENT,
        IDENTIFICATION,
        MEMBER_NUMBER,
        AMOUNT,
        PAYMENT_DATE,
        CATEGORY,
        BANK_ACCOUNT,
        OWNER,
        TAX_INFORMATION,
        REMARK
    };
}